# 🚀 Инструкция по запуску проекта

Этот проект можно запустить на любой ОС: **Windows**, **Linux**, **macOS**.  
Ниже — пошаговые инструкции.

---

## 🧰 Перед началом

Убедитесь, что установлены:
- **Python 3.9–3.11**  
  Скачать: [python.org](https://www.python.org/downloads/)
- **Git**  
  Скачать: [git-scm.com](https://git-scm.com/)

> 💡 Рекомендуется использовать виртуальное окружение для изоляции зависимостей.

## 🪟 Windows (любой терминал)

1. Откройте командную строку, PowerShell или терминал в вашей IDE

2. Клонируйте репозиторий:
3. 
   git clone https://github.com/ваш-аккаунт/ml-data-pipeline.git
   
   cd ml-data-pipeline
   
Создайте и активируйте виртуальное окружение:

python -m venv venv

venv\Scripts\activate

Установите зависимости:

pip install -r requirements.txt

Запустите приложение:

python main_window_ui.py

## 🚀 Как запустить (Linux)

🐧 Linux / 🍏 macOS

Откройте терминал

Клонируйте репозиторий:

git clone https://github.com/ваш-аккаунт/ml-data-pipeline.git

cd ml-data-pipeline
Создайте и активируйте виртуальное окружение:

python3 -m venv venv

source venv/bin/activate

Установите зависимости:

pip install -r requirements.txt

Запустите приложение:

python src/main_window_ui.py

## 🚀 Как запустить в VS Code 
▌ Инструкция по запуску проекта на Windows в Visual Studio Code

▌ Шаг 1. Установка Git и Python

Если ещё не установлены, установите Git и Python. Скачать их можно отсюда:

- Git: git-scm.com (https://git-scm.com/download/win)
- Python: python.org (https://www.python.org/)

▌ Шаг 2. Открытие VS Code

Откройте VS Code и перейдите в папку рабочего пространства (File > Open Folder).

▌ Шаг 3. Клонирование репозитория

Откройте терминал в VS Code (Terminal > New Terminal) и выполните команду:

git clone https://github.com/ваш-аккаунт/ml-data-pipeline.git
cd ml-data-pipeline

▌ Шаг 4. Создание виртуального окружения

Создайте виртуальное окружение прямо в терминале VS Code:

python -m venv venv

▌ Шаг 5. Активация виртуального окружения

Для активации виртуального окружения выполните следующую команду:

.\\venv\\Scripts\\activate

Теперь вы увидите название вашего виртуального окружения слева от приглашения командной строки.

▌ Шаг 6. Установка зависимостей

Установите необходимые пакеты Python командой:

pip install -r requirements.txt

▌ Шаг 7. Запуск приложения

Наконец, запустите ваше приложение:

python main_window_ui.py

Приложение должно успешно запуститься!
